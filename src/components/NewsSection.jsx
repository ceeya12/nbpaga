import React from 'react';
import './NewsSection.css'; // Custom CSS for styling

function NewsSection({children}) {
    return (
        <>
            {children}
        </>

    );
}

export default NewsSection;